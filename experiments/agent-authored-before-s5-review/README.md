# Historical first authoring exercise

This is the original agent-authored mending-circle tool desk and its complete
26-call transcript. It was created after the S6 host started, under the first
S5 candidate's runtime profile. The separate S5 review then found and corrected
a signed-closure determinism bug, changing that runtime CID.

This directory preserves the first run unchanged. Its archive requires the
installed runtime at S5 candidate `757c76f54746d3ab1577a9d08819069525077fab`.
It is not presented as replayable by the corrected S5 runtime. The agent's repeat
publication and complete current-runtime evidence are under `../agent-authored/`.
The first run records the anonymous-preview and missing invitation-link usability
gaps; neither is hidden by the successful repeated exercise.
